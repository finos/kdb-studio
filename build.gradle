apply plugin: 'idea'
//and standard one
apply plugin: 'java'
apply plugin: 'application'

compileJava {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.netbeans.api', name: 'org-openide-awt', version: 'RELEASE150'
    compile group: 'org.jfree', name: 'jfreechart', version: '1.5.3'
    compile group: 'org.apache.poi', name: 'poi', version: '5.2.3'
    compile group: 'org.apache.poi', name: 'poi-ooxml', version: '5.2.3'
    compile group: 'com.fasterxml.jackson.core', name: 'jackson-databind', version:'2.14.0-rc2'
}

sourceSets {
    main {
        java {
            srcDirs 'src'
        }
        resources {
            srcDir 'src'
            include 'studio*.properties'
            include 'org/netbeans/editor/Bundle*.properties'
            include 'images/**'
        }
    }
}

application {
    mainClassName = 'studio.core.Studio'
}

jar {
    manifest {
        attributes 'Main-Class': 'studio.core.Studio'
    }
}
